import { Canvas, Meta, Story } from "@storybook/addon-docs"
import All from './components/tokens/All.vue'
import Color from './components/tokens/Color.vue'
import FontSize from './components/tokens/FontSize.vue'
import Spacing from './components/tokens/Spacing.vue'

<Meta title="Basics/Tokens" />

export const ColorTemplate = () => ({
  components: { Color },
  template: `<color />`
})

export const FontTemplate = () => ({
  components: { FontSize },
  template: `<font-size />`
})

export const SpacingTemplate = () => ({
  components: { Spacing },
  template: `<spacing />`
})

export const AllTokensTemplate = () => ({
  components: { All },
  template: `<all />`
})

# ownCloud Design System tokens

Design tokens are the atoms of the system as [Salesforce](https://www.lightningdesignsystem.com/design-tokens/) describes them. In ownCloud Design System they are used instead of hard coded values to ensure a better consistency across any platform.


You’re looking at ownCloud Design System’s demo <code>design tokens</code>. Everything you see here is editable in Markdown format. To change or remove this content, see [/docs/tokens.md](https://github.com/owncloud/owncloud-design-system/blob/master/docs/tokens.md). The documentation you see below is auto generated by ownCloud Design System. To edit how it’s formatted, see [/docs/components/tokens/](https://github.com/owncloud/owncloud-design-system/blob/master/docs/components/tokens).

## Color tokens
<Canvas>
  <Story
    name="Color"
  >
    {ColorTemplate.bind({})}
  </Story>
</Canvas>

## FontSize tokens
<Canvas>
  <Story
    name="Fontsize"
  >
    {FontTemplate.bind({})}
  </Story>
</Canvas>

## Spacing tokens
<Canvas>
  <Story
    name="Spacing"
  >
    {SpacingTemplate.bind({})}
  </Story>
</Canvas>

## All tokens
<Canvas>
  <Story
    name="All"
  >
    {AllTokensTemplate.bind({})}
  </Story>
</Canvas>
