import { Canvas, Meta, Story } from "@storybook/addon-docs"

import OcButton from "./OcButton.vue"
import OcTableSimple from "../OcTableSimple/OcTableSimple.vue"

<Meta title="Atoms/Button/Examples" component={OcButton} />

export const Button = (args, { argTypes }) => ({
  props: Object.keys(argTypes),
  components: { OcButton },
  data() {
    return {
      args: { ...args }
    }
  },
  template: `<oc-button v-bind="args">{{args.text}}</oc-button>`,
})

export const ButtonVariations = (args, { argTypes }) => ({
  props: Object.keys(argTypes),
  components: { OcButton },
  data() {
    return {
      args: { ...args }
    }
  },
  template: `
  <div>
    <oc-button v-bind="args" variation="passive">passive</oc-button>
    <oc-button v-bind="args" variation="primary">primary</oc-button>
    <oc-button v-bind="args" variation="success">success</oc-button>
    <oc-button v-bind="args" variation="warning">warning</oc-button>
    <oc-button v-bind="args" variation="danger">danger</oc-button>
   </div>
  `
})

export const ButtonSizes = (args, { argTypes }) => ({
  props: Object.keys(argTypes),
  components: { OcButton },
  data() {
    return {
      args: { ...args }
    }
  },
  template: `
  <div>
    <oc-button v-bind="args" size="large" class="oc-mr-s">Large</oc-button>
    <oc-button v-bind="args" class="oc-mr-s oc-align-self-center">Medium (default)</oc-button>
    <oc-button v-bind="args" size="small" class="oc-align-self-center">Small</oc-button>
   </div>
  `
})

export const AllButtons = (args, { argTypes }) => ({
  components: { OcButton, OcTableSimple },
  data() {
    return {
      args: { ...args },
    }
  },
  computed: {
    variations() {
      [
        {
          title: "passive",
          description: "Default appearance of interactive elements (links and buttons)"
        },
        {
          title: "primary",
          description: "Used as a visual clue to the main action on each page"
        },
        {
          title: "success",
          description: "Useful as confirmations or to finish a multi-step process"
        },
        {
          title: "warning",
          description: "Used as a visual hint for certain actions, like publicly sharing a file or removing a share"
        },
        {
          title: "danger",
          description: "Use for triggering possible destructive and non-reversible actions, like permanently deleting a file"
        },
        {
          title: "inverse",
          description: "Use on a dark background"
        }
      ]
    },
    appearances() {
      [
        "outline",
        "filled",
        "raw"
      ]
    }
  },
  template: `
    <oc-table-simple>
      <oc-thead>
        <oc-tr>
          <oc-th>Variation &amp; usage</oc-th>
          <oc-th>Outline (default)</oc-th>
          <oc-th>Filled</oc-th>
          <oc-th>Raw</oc-th>
        </oc-tr>
      </oc-thead>
      <oc-tbody>
        <oc-tr v-for="variation in variations">
        <oc-td>
          {{ variation.description }}
        </oc-td>
          <oc-td v-for="appearance in appearances" :class="{ 'oc-background-brand': variation.title === 'inverse' }" class="oc-p-m">
            <oc-button :variation="variation.title" :appearance="appearance" class="oc-mb-s">
              {{ variation.title }}
              <oc-icon name="home" />
            </oc-button>
            <oc-button :variation="variation.title" :appearance="appearance" disabled>
              {{ variation.title }}
              <oc-icon name="home" />
            </oc-button>
          </oc-td>
        </oc-tr>
      </oc-tbody>
    </oc-table-simple>
  `
})

# Examples for OcButton

## Button variations

<Canvas>
  <Story
    name="Variations"
  >
    {ButtonVariations.bind({})}
  </Story>
</Canvas>

##  Button sizes 

<Canvas>
  <Story
    name="Sizes"
  >
    {ButtonSizes.bind({})}
  </Story>
</Canvas>

##  Button with icons

##  Using buttons in a group

<Canvas>
  <Story
    name="Passive"
    args={{
      text: "Passive",
      variation: "passive",
    }}
  >
    {Button.bind({})}
  </Story>
</Canvas>


## All available button combinations

<Canvas>
  <Story
    name="All"
    >
    {AllButtons.bind({})}
  </Story>
</Canvas>
